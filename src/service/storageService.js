var supabaseClient = require('./../client/supabaseClient');

async function salvarImagem(data) {

  

  try {
    console.log()
  } catch (error) {
    //vá pra frente
  }
}

module.exports = {
  salvarImagem
};